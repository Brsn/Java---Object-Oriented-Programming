int i = root(p);
int j = root(q);

if(size[i] < size[j]) { id[i] = j; size[j] += size[i]; }
   //if i is less than j, i equals j. size j equals j + i.

else { id[j] = i; size[i] += size[j]; }
  // else j equals i. the size of i equals i + j.

